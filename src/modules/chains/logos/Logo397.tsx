export const Logo397 = ({ size = 15 }: { size?: number }) => {
  return (
    <div>
      <img
        src="/chains/397.svg"
        alt="NEAR"
        style={{ width: `${size}px`, height: `${size}px` }}
      />
    </div>
  );
};

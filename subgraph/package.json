{
  "name": "@ecospatial-vault/subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ecospatial-vault",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ecospatial-vault",
    "create:local": "graph create --node http://localhost:8020/ ecospatial-vault",
    "remove:local": "graph remove --node http://localhost:8020/ ecospatial-vault",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}

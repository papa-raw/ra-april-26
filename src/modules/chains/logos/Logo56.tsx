export const Logo56 = ({ size = 20 }: { size?: number }) => {
  return (
    <div>
      <img
        src="/chains/56.svg"
        alt="BNB Chain"
        style={{ width: `${size}px`, height: `${size}px` }}
      />
    </div>
  );
};

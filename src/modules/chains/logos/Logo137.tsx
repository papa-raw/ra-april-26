export const Logo137 = ({ size = 28 }: { size?: number }) => {
  return (
    <div>
      <img
        src="/chains/137.svg"
        alt="Polygon"
        style={{ width: `${size}px`, height: `${size}px` }}
      />
    </div>
  );
};

export const Logo8453 = ({ size = 20 }: { size?: number }) => {
  return (
    <div>
      <img
        src="/chains/8453.svg"
        alt="Base"
        style={{ width: `${size}px`, height: `${size}px` }}
      />
    </div>
  );
};
